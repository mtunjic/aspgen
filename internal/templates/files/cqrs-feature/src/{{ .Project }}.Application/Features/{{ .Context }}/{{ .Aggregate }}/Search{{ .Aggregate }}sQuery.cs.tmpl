namespace {{ .Project }}.Application.Features.{{ .Context }}.{{ .Aggregate }};

public sealed record Search{{ .Aggregate }}sQuery(string? Search{{ filterParamDecls .Properties "pascal" }}, int Page, int PageSize);
