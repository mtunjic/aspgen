namespace {{ .Project }}.Application.Features.{{ .Context }}.{{ .Aggregate }};

public sealed record {{ .Aggregate }}PagedResponse(IReadOnlyList<{{ .Aggregate }}View> Items, long TotalCount, int Page, int PageSize);
