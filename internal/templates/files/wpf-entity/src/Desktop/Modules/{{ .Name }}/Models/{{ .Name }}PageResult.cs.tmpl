namespace {{ .Namespace }}.Models;

public sealed record {{ .Name }}PageResult(IReadOnlyList<{{ .Name }}Row> Items, long TotalCount, int Page, int PageSize)
    : global::{{ .Project }}.Desktop.Shared.IListPageResult<{{ .Name }}Row>
{
    public bool HasMore => (long)Page * PageSize < TotalCount;
}
