namespace {{ .Namespace }}.Models;

public sealed record {{ .Name }}Row(
    long Id{{ range .Properties }}, {{ .CSharpType }} {{ .Name }}{{ end }}) : global::{{ .Project }}.Desktop.Shared.IEntityRow;
