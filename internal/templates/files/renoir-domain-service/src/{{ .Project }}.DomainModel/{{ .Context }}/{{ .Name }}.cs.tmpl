namespace {{ .Project }}.DomainModel.{{ .Context }};

// Stateless domain policy. It coordinates domain objects when behavior does not belong
// naturally to one entity or aggregate root.
public sealed class {{ .Name }}
{
    // Add ubiquitous-language operations here. Keep infrastructure concerns outside the domain.
}
