using {{ .Project }}.DomainModel;

namespace {{ .Project }}.DomainModel.Contexts.{{ .Context }}.Aggregates;

// Repository contract belongs to the domain; its implementation belongs to persistence.
public interface I{{ .Name }}
{
    Task<{{ .Aggregate }}?> GetByIdAsync(long id, CancellationToken cancellationToken = default);
    Task<IReadOnlyList<{{ .Aggregate }}>> GetAllAsync(CancellationToken cancellationToken = default);
    Task<CommandResponse> AddAsync({{ .Aggregate }} aggregate, CancellationToken cancellationToken = default);
    Task<CommandResponse> SaveAsync({{ .Aggregate }} aggregate, CancellationToken cancellationToken = default);
    Task<CommandResponse> DeleteAsync(long id, CancellationToken cancellationToken = default);
}
